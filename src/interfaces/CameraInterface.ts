export interface CameraInterface {
    cameraId: string,
    userId: string,
    cameraNum: number;
    cameraName: string;
    cameraType: string;
    cameraUsage: string;
    cameraOrientation: string;
    city: string;
    location: string;
}
