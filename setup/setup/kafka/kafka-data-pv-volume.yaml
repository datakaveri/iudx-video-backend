apiVersion: v1
kind: PersistentVolume
metadata:
  name: kafka-data-pv-volume
  labels:
    type: local
spec:
  storageClassName: ""
  capacity:
    storage: 50Mi
  accessModes:
    - ReadWriteOnce
  claimRef:
     namespace: default
     name: kafka-data-pv-claim
  hostPath:
    path: "/mnt/kafkadata"